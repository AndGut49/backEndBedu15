class Categoria {
    constructor (id, nombre, descripcion) {
        this.id = id;
        this.nombre = nombre;
        this.telefono = telefono; //Revisar por qué hay un atributo teléfono en este modelo
        this.descripcion = descripcion;
    }

    crearCategoria () {
        // Función para crear una nueva categoria
    }
    actualizarCategoria () {
        // Función para actualizar la información de una categoria
    }
    eliminarCategoria () {
        //Función para eliminar una categoria
    } 

    consultarCategorias () {
        //Función para consultar las categorias que se tienen
    }
}
module.exports = Categoria;